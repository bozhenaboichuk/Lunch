%article.card
  %section
    .block
      =image_tag restaurant.picture || ''
    .block
      = restaurant.name
    .block
      = restaurant.address
    .block
      = restaurant.phone_number
  %section.mt-2
    = link_to 'Show', restaurant_path(restaurant), class: 'btn'
